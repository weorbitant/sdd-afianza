# Legacy workflows

Archived. Not executed by the runner.

- `speckit.yml` — original GitHub Spec Kit "full SDD cycle" tutorial workflow. Superseded by the split `refinement/` + `planning/` + `implementation/` workflows, which respect the Afianza Jira-first model (see `.specify/REFINEMENT.md`).
